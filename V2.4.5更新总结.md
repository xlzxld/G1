# Hot Runner MES - V2.4.5 更新总结

为了解决原本客户与外协厂商“只能录入一位联系人”的业务局限，并打通原本在列表、详情、新建表单中遗漏展示联系人信息的痛点，我们在本次 **V2.4.5** 版本中对系统的联系人架构进行了全栈式升级。

本更新文件总结了本次版本迭代中所完成的所有数据库结构设计、后端路由、数据兼容层、测试种子脚本重构以及前端 Element Plus 页面的优化历程。

---

## 📖 核心业务需求

1. **多联系人支持**：客户（Customers）与外协厂商（Vendors）支持录入多位联系人，用以承载不同部门（如采购、技术、财务、品质）的对接需求。
2. **动态联系方式**：每位联系人内部支持录入多个类型可选、动态增删的联系方式（如：电话、微信、邮箱、QQ 等）。
3. **高兼容性设计**：改造必须向后兼容，自动提取主联系人信息同步写入 legacy 物理列，确保老订单、数据报表及仪表台（Dashboard）渲染正常且不报错。
4. **稳定排序**：保证增删改后，客户/厂商列表数据顺序稳固，默认以名称（字典字母序）排序。

---

## 🛠 全栈技术修改明细

### 1. 数据库与表结构迁移 (Database & Migrations)
* **模型扩容** [models.py](file:///Users/xlz/Documents/G1-V2/server-python/models.py)：
  * 在 `Customer` 与 `Vendor` 数据表中新增 `contacts = Column(JSON, default=list)` 列，以高弹性的 JSON 结构承载结构化多联系人数据。
* **物理自动迁移** [main.py](file:///Users/xlz/Documents/G1-V2/server-python/main.py)：
  * 在服务启动块中增加 `ALTER TABLE customers ADD COLUMN IF NOT EXISTS contacts JSON DEFAULT '[]';` 及 `ALTER TABLE vendors ADD COLUMN IF NOT EXISTS contacts JSON DEFAULT '[]';`，使得系统在更新部署时自动扩容列，无需手动干预数据库。

### 2. 接口协议定义 (API Schemas)
* **嵌套 Schema 重构** [schemas.py](file:///Users/xlz/Documents/G1-V2/server-python/schemas.py)：
  * 定义 `ContactMethodSchema`（包含 `type` 和 `value` 字段）。
  * 定义 `ContactSchema`（包含 `name` 姓名、`role` 职务，以及嵌套的 `contact_methods: List[ContactMethodSchema]` 数组）。
  * 将 `contacts: Optional[List[ContactSchema]]` 绑定至 `CustomerBase` 和 `VendorBase` 基础模型中。

### 3. 后端路由控制与向后兼容 (Routers)
* **首联系人提取与同步** [customers.py](file:///Users/xlz/Documents/G1-V2/server-python/routers/customers.py) & [vendors.py](file:///Users/xlz/Documents/G1-V2/server-python/routers/vendors.py)：
  * 拦截 `POST` 新建与 `PUT` 更新路由，强制校验联系人姓名及下属联系方式非空。
  * **兼容逻辑**：如果录入了联系人列表，系统将提取首个联系人的 `name` 赋值给 legacy 的 `contact` 字段；并遍历其动态联系方式列表，优先匹配 `“电话”` 类别（若无则取首个值）同步写入 legacy `phone` 字段。这完美保证了 Dashboard 等已有卡片的数据连贯性。
* **默认名称排序实现**：
  * 在 `get_customers` 和 `get_vendors` 的查询逻辑中，显式追加 `.order_by(models.Customer.name)` 与 `.order_by(models.Vendor.name)` 排序参数。解决因数据库物理行修改造成列表顺序跳动的问题。

### 4. 种子脚本测试数据重构 (Seed Data)
* **全面覆盖** [seed_mock.py](file:///Users/xlz/Documents/G1-V2/server-python/seed_mock.py)：
  * 修改了 5 个初始客户与 3 个外协供应商的种子数据。**为每一家厂商/客户均分配了 2 到 4 个联系人**，并给每名联系人配置了动态的“电话+微信+邮箱”联系方式列表，极大丰富了开发阶段的数据表现力。

---

## 🎨 前端交互与视觉打磨过程 (UI & UX)

在前端 Element Plus 页面改造与调优期间，我们历经了三轮优化打磨：

### 1. 多联系人信息解析与列表渲染
* **列表视图重构** [Customers.vue](file:///Users/xlz/Documents/G1-V2/client/src/views/Customers.vue) & [Outsourcing.vue](file:///Users/xlz/Documents/G1-V2/client/src/views/Outsourcing.vue)：
  * 在桌面端 `el-table` 中将“联系方式”列升级为“联系人列表”列，结构化显示每个联系人的“姓名 (职务)”，其下细分显示所有可选的联系方式标签。
  * 移动端卡片式列表同步支持多联系人的多行小字解析排版。
* **详情卡片网格** [CustomerDetail.vue](file:///Users/xlz/Documents/G1-V2/client/src/views/CustomerDetail.vue)：
  * 重构基础字段描述表，并在详情面板正下方开辟了干净整洁的 **“联系人列表”卡片网格**。采用柔和底色区隔不同的对接人，并将其名下的联系电话、微信、邮箱等信息以键值对齐展示。

### 2. 弹窗交互优化与脏数据隔离
* **弹窗数据深拷贝 (Deep Clone)**：
  * 在打开编辑对话框（`openEdit`）时，对行数据中的 `contacts` 使用 `JSON.parse(JSON.stringify(...))` 进行深度拷贝克隆。彻底修复了“在表单中增加或修改联系人，点击取消按钮后，脏数据依然残留污染主列表”的经典内存引用溢出问题。
* **统一删除按钮 UI 规范**：
  * 将联系人表单卡片右上角不美观的绝对定位小红叉剔除，改为在卡片底部放置正规居右的红框 `“删除该联系人”` 按钮。
  * 将具体联系方式右侧的文字链“删除”统一重构为 `type="danger" size="small" plain` 扁平红框小按钮，使其具备明确的交互反馈。

### 3. 老数据/缺省状态防崩溃降级处理
* 为了防止测试数据库在迁移或手工调试时出现缺少 `contact_methods` 数组导致前端循环语句崩溃的问题，在 `parseContacts` 解析函数中做了防御性兼容设计。系统会自动感知并兼容 `{ phone, wechat, email }` 这一过渡期数据结构并安全转换，确保 PC 端与移动端界面稳定运行，永不报红。

---

## 📂 本次更新涉及的代码变更文件汇总

* 💻 **后端逻辑**：
  * [models.py](file:///Users/xlz/Documents/G1-V2/server-python/models.py) —— 数据库表定义
  * [main.py](file:///Users/xlz/Documents/G1-V2/server-python/main.py) —— 自动迁移脚本
  * [schemas.py](file:///Users/xlz/Documents/G1-V2/server-python/schemas.py) —— 数据传输协议
  * [routers/customers.py](file:///Users/xlz/Documents/G1-V2/server-python/routers/customers.py) —— 客户路由层
  * [routers/vendors.py](file:///Users/xlz/Documents/G1-V2/server-python/routers/vendors.py) —— 外协路由层
  * [seed_mock.py](file:///Users/xlz/Documents/G1-V2/server-python/seed_mock.py) —— 测试种子数据注入
* 🎨 **前端组件**：
  * [Customers.vue](file:///Users/xlz/Documents/G1-V2/client/src/views/Customers.vue) —— 客户管理列表与弹窗表单
  * [Outsourcing.vue](file:///Users/xlz/Documents/G1-V2/client/src/views/Outsourcing.vue) —— 外协管理列表与弹窗表单
  * [CustomerDetail.vue](file:///Users/xlz/Documents/G1-V2/client/src/views/CustomerDetail.vue) —— 客户详情页联系人卡片网格
