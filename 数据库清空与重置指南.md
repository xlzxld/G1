# 数据库清空与重置指南

本文档说明如何清空本项目数据库数据，以及不同方式的适用场景和风险。

项目默认使用 Docker Compose 运行 PostgreSQL，数据库数据保存在 Docker volume 中。清空数据前请确认当前环境是开发环境还是正式环境，正式环境操作前必须先备份。

## 方案一：彻底清空 Docker 数据卷

这是最干净的方式，会删除 PostgreSQL 数据卷，数据库回到全新状态。

适用场景：

- 本地开发环境想完全重置。
- 数据库结构或数据已经混乱。
- 不需要保留任何用户、客户、订单、库存、图纸记录。

命令：

```bash
docker compose down -v
docker compose up -d
docker compose exec backend python seed.py
```

如果还需要注入演示业务数据：

```bash
docker compose exec backend python seed_mock.py
```

如果上传文件也要一起清空：

```bash
rm -rf server-python/uploads
```

注意：

- `docker compose down -v` 会删除数据库 volume。
- 所有数据库表数据都会丢失。
- 正式环境不要直接执行，除非已经确认备份并接受数据清空。

## 方案二：只清空数据库表，保留容器和数据卷

这种方式不删除 Docker 容器和 volume，只把数据库表里的数据清空。

适用场景：

- 不想重建 Docker 容器。
- 只想清空数据库内容。
- 想保留当前 Docker volume。

进入数据库：

```bash
docker compose exec db psql -U mes_user -d hotrunner_mes
```

在 psql 中执行：

```sql
TRUNCATE TABLE
  audit_logs,
  notifications,
  notification_rules,
  documents,
  inventory_reservations,
  process_steps,
  process_flows,
  orders,
  inventory_items,
  vendors,
  customers,
  page_permissions,
  users,
  system_settings
RESTART IDENTITY CASCADE;
```

退出 psql：

```sql
\q
```

重新初始化基础账号和权限：

```bash
docker compose exec backend python seed.py
```

如果需要演示数据：

```bash
docker compose exec backend python seed_mock.py
```

注意：

- `RESTART IDENTITY` 会重置自增 ID。
- `CASCADE` 会处理外键依赖。
- 这种方式不会自动删除磁盘上的 `server-python/uploads` 文件。

## 方案三：只重置演示业务数据，保留用户账号

当前项目已有 `seed_mock.py`。它会清空并重新生成一批演示业务数据，但不会清空基础用户和权限。

适用场景：

- 想保留 `admin`、`laowang`、`xiaoli` 等账号。
- 只想刷新客户、订单、库存、流程、外协、通知等演示业务数据。
- 不想删除系统基础权限。

命令：

```bash
docker compose exec backend python seed_mock.py
```

注意：

- `seed_mock.py` 会清理旧演示业务数据。
- 它不等同于完整清库。
- 如果旧数据不是由演示脚本产生，也可能被一并清掉业务表数据。

## 方案四：新增专用 reset_db.py 脚本

不建议把“清空全部数据”的逻辑直接塞进 `seed.py`，因为 `seed.py` 是基础初始化脚本，误执行会导致账号和权限被清空。

更推荐新增独立脚本：

```text
server-python/reset_db.py
```

用途：

- 明确表示这是危险操作。
- 专门负责清空全部表。
- 清空后再手动执行 `seed.py` 和 `seed_mock.py`。

推荐执行顺序：

```bash
docker compose exec backend python reset_db.py
docker compose exec backend python seed.py
docker compose exec backend python seed_mock.py
```

建议脚本行为：

- 打印危险提示。
- 只在 `APP_ENV=development` 或明确传入 `--yes` 时执行。
- 按外键依赖顺序清空表。
- 使用 `TRUNCATE ... RESTART IDENTITY CASCADE`。
- 不自动删除 uploads，除非额外传入参数。

## 方案五：正式环境先备份再清空

正式环境不要直接清库。正确流程是：

1. 停止写入或通知用户维护窗口。
2. 备份 PostgreSQL。
3. 备份 `uploads/`。
4. 验证备份文件存在。
5. 执行清库。
6. 重新初始化基础数据。
7. 登录系统验证。

数据库备份示例：

```bash
docker compose exec db pg_dump -U mes_user -d hotrunner_mes -Fc -f /tmp/hotrunner_mes.dump
docker compose cp db:/tmp/hotrunner_mes.dump ./hotrunner_mes.dump
```

上传文件备份示例：

```bash
tar -czf uploads-backup.tar.gz server-python/uploads
```

## 推荐选择

本地开发最推荐：

```bash
docker compose down -v
docker compose up -d
docker compose exec backend python seed.py
docker compose exec backend python seed_mock.py
```

只想刷新演示数据：

```bash
docker compose exec backend python seed_mock.py
```

想长期保留一键清库能力：

- 新增 `server-python/reset_db.py`。
- 不要把危险清库逻辑直接放进 `seed.py`。

## 风险提醒

- `docker compose down -v` 会删除数据库 volume。
- `TRUNCATE ... CASCADE` 会清空表数据并处理外键依赖。
- 清库不会自动删除 `uploads/`，数据库记录删除后，磁盘文件可能变成孤儿文件。
- 正式环境清库前必须备份数据库和 uploads。
- 如果未来拆分为 `docker-compose.dev.yml` 和 `docker-compose.prod.yml`，命令中的 `docker compose` 应按环境增加 `-f` 参数，例如：

```bash
docker compose -f docker-compose.dev.yml down -v
docker compose -f docker-compose.dev.yml up -d
```
