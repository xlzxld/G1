# 汇易通 MES V2 分阶段实施计划

## 使用方式

这份文档把 [项目优化与部署计划.md](./项目优化与部署计划.md) 拆成多个可单独实施、可单独验收的小计划。建议一个小计划对应一个独立 PR 或一次独立提交，避免把 Docker、上传、数据库、部署、测试一次性混在一起。

推荐执行顺序：

```text
P0 基线梳理
  -> P1 环境变量拆分
  -> P2 开发环境 Docker
  -> P3 正式环境 Docker + 反向代理
  -> P4 移动端直拍上传
  -> P5 前端性能小优化
  -> P6 后端安全与健康检查
  -> P7 后端查询性能优化
  -> P8 数据库迁移与索引
  -> P9 备份与恢复
  -> P10 自动化部署
  -> P11 测试体系
  -> P12 文档与一致性整理
```

其中 P4 可以在 P1 之后并行做；P9、P10 最好在 P3 后做；P11 可以按模块逐步补，不必等全部功能完成。

## P0. 基线梳理与风险冻结

### 目标

在开始改造前固定当前项目状态，确认现有开发启动、默认账号、接口和数据结构，避免后续拆分 Docker 或迁移数据库时不知道问题是旧问题还是新问题。

### 范围

- 不改业务逻辑。
- 不做生产部署。
- 只做基线确认和记录。

### 涉及文件

- `README.md`
- `DOCKER_GUIDE.md`
- `项目优化与部署计划.md`
- 可新增 `docs/current-baseline.md`

### 实施步骤

1. 记录当前服务结构：前端、后端、数据库、端口映射。
2. 记录默认账号：`admin / 123`、`laowang / 123`、`xiaoli / 123`。
3. 记录当前启动命令和播种命令。
4. 记录当前已知问题：
   - Docker 只有一份配置，开发和正式混在一起。
   - 后端生产仍会使用 reload 风格。
   - JWT `SECRET_KEY` 写死。
   - `/auth/me` 有 fallback 到用户 1 的风险。
   - 文档部分接口和代码不同步。
   - 没有自动化测试目录。
5. 确认 `.gitignore` 包含 `.env`、`uploads/`、构建产物和缓存目录。

### 验收标准

- 有一份明确的当前状态记录。
- 后续改造时可以回看基线判断回归来源。
- 没有改变运行行为。

## P1. 环境变量与配置分层

### 目标

先把开发和正式环境需要的配置项从代码和 Docker 文件里抽出来，为后续 Docker 拆分、正式部署、CI/CD 做准备。

### 范围

- 新增环境变量模板。
- 后端读取环境变量。
- 前端保持相对路径 `/api`，不再依赖写死局域网 IP。
- 不启动正式部署。

### 涉及文件

- `.env.example`
- `.env.dev`
- `.env.prod.example`
- `.gitignore`
- `server-python/database.py`
- `server-python/routers/auth.py`
- `server-python/main.py`
- `docker-compose.yml` 后续会在 P2/P3 拆分，此阶段只定义变量清单。

### 实施步骤

1. 新增 `.env.example`，列出通用变量：
   - `APP_ENV`
   - `DATABASE_URL`
   - `POSTGRES_USER`
   - `POSTGRES_PASSWORD`
   - `POSTGRES_DB`
   - `SECRET_KEY`
   - `CORS_ORIGINS`
   - `UPLOAD_DIR`
   - `BACKUP_DIR`
   - `PUBLIC_BASE_URL`
2. 新增 `.env.prod.example`，明确正式环境必须修改：
   - 强随机 `SECRET_KEY`
   - 强数据库密码
   - 真实域名或公网访问地址
   - 正式 CORS 白名单
3. 新增 `.env.dev` 或 `.env.dev.example`：
   - 开发环境可使用默认弱密码。
   - 标注不能直接用于正式环境。
4. 后端认证模块从环境变量读取 `SECRET_KEY`。
5. CORS 从 `CORS_ORIGINS` 读取，开发环境可放宽，正式环境必须限定域名。
6. 上传目录从 `UPLOAD_DIR` 读取，默认仍为 `uploads`。
7. 确认 `.env.prod` 不被提交。

### 验收标准

- 代码中不再硬编码正式环境密钥。
- `.env.example` 能说明所有必要配置。
- 正式配置模板和开发配置模板有明确区别。
- 不影响当前开发环境启动。

## P2. 开发环境 Docker 拆分

### 目标

把当前开发用 Docker 配置整理成专门的开发环境配置，保留热更新、源码挂载、Swagger、数据库本地连接能力。

### 范围

- 新增开发 compose 和开发 Dockerfile。
- 保持当前开发体验。
- 不处理正式 HTTPS。

### 涉及文件

- `docker-compose.dev.yml`
- `client/Dockerfile.dev`
- `server-python/Dockerfile.dev`
- `client/vite.config.js`
- `DOCKER_GUIDE.md`

### 实施步骤

1. 从现有 `docker-compose.yml` 拆出 `docker-compose.dev.yml`。
2. 前端开发配置：
   - 使用 `client/Dockerfile.dev`。
   - 运行 `npx vite --host 0.0.0.0 --port 5173`。
   - 暴露 `9956:5173`。
   - 挂载 `./client:/app`。
   - 保留 `/app/node_modules` volume。
3. 后端开发配置：
   - 使用 `server-python/Dockerfile.dev`。
   - 运行 `uvicorn main:app --host 0.0.0.0 --port 8000 --reload`。
   - 暴露 `8080:8000`。
   - 挂载 `./server-python:/app`。
4. 数据库开发配置：
   - 可暴露 `5432:5432`，方便本地数据库工具连接。
   - 使用独立 dev volume，例如 `postgres_dev_data`。
5. Vite proxy 保持：
   - `/api` -> `http://backend:8000`
   - `/uploads` -> `http://backend:8000`
6. 移除开发 compose 中硬编码局域网 IP。
7. 在 `DOCKER_GUIDE.md` 写清开发命令。

### 验收标准

- `docker compose -f docker-compose.dev.yml up -d` 可启动。
- 前端 `http://localhost:9956` 可访问。
- 后端 `http://localhost:8080/docs` 可访问。
- `seed.py` 和 `seed_mock.py` 可执行。
- 修改前后端源码后可热更新。

## P3. 正式环境 Docker、静态前端与反向代理

### 目标

建立正式环境运行方式：前端静态构建、后端无 reload、数据库不暴露公网、Caddy/Nginx 统一入口。

### 范围

- 新增正式 compose。
- 新增正式 Dockerfile。
- 新增 Caddy 或 Nginx 配置。
- 不做自动化部署，自动化放到 P10。

### 涉及文件

- `docker-compose.prod.yml`
- `client/Dockerfile.prod`
- `server-python/Dockerfile.prod`
- `Caddyfile.prod` 或 `nginx.prod.conf`
- `.env.prod.example`
- `docs/deployment.md`

### 实施步骤

1. 新增 `client/Dockerfile.prod`：
   - 安装依赖。
   - 执行 `npm run build`。
   - 输出静态文件给 Caddy/Nginx。
2. 新增 `server-python/Dockerfile.prod`：
   - 安装 Python 依赖。
   - 不使用 `--reload`。
   - 默认命令为 `uvicorn main:app --host 0.0.0.0 --port 8000 --workers 2`。
3. 新增 `docker-compose.prod.yml`：
   - `db` 不映射 `5432` 到宿主机公网。
   - `backend` 不映射 `8000` 到公网，只给 proxy 访问。
   - `frontend/proxy` 暴露 `80/443`。
   - `uploads` 持久化。
   - `backups` 持久化。
   - 所有服务 `restart: unless-stopped`。
4. 增加 healthcheck：
   - db 使用 `pg_isready`。
   - backend 使用 `/healthz`。
   - proxy 检查前端入口。
5. 反向代理规则：
   - `/` 返回前端静态文件。
   - `/api/*` 反代到后端。
   - `/uploads/*` 提供上传文件访问。
6. 域名 HTTPS：
   - Caddy 自动签发证书。
   - `PUBLIC_BASE_URL` 设置为 `https://your-domain.com`。
7. IP+端口 fallback：
   - 文档中说明只用于临时访问。
   - 长期仍推荐域名 + HTTPS。

### 验收标准

- `docker compose -f docker-compose.prod.yml --env-file .env.prod up -d --build` 可启动。
- 前端通过 proxy 可访问。
- `/api/healthz` 可访问。
- `/uploads/...` 可访问已有上传文件。
- 公网不暴露 `5432`、`8000`、`5173`。

## P4. 移动端相机直拍、预览与压缩上传

### 目标

解决移动端上传图纸和工序照片只能从相册选择的问题，增加直接拍照、预览、压缩后上传能力，同时保留原有文件选择。

### 范围

- 改前端上传交互。
- 后端上传接口保持 `POST /documents` 不变。
- 工程文件上传不受影响。

### 涉及文件

- `client/src/components/DrawingPanel.vue`
- `client/src/views/OrderDetail.vue`
- 可新增 `client/src/utils/imageUpload.js`
- 可新增前端测试文件。

### 实施步骤

1. 抽出图片处理 helper：
   - 判断是否图片。
   - 读取图片为预览 URL。
   - Canvas 压缩。
   - 压缩失败 fallback 原图。
2. 图纸上传弹窗增加两个入口：
   - “拍照上传”：`input type="file" accept="image/*" capture="environment"`。
   - “从相册/文件选择”：保留现有 `el-upload` 或普通文件选择。
3. 工序照片弹窗增加同样的拍照入口。
4. 拍照或选图后显示预览。
5. 用户点击确认后：
   - 图片先压缩。
   - 再提交 `multipart/form-data`。
6. 压缩规则：
   - 长边最大 `1920px`。
   - JPEG 质量 `0.82`。
   - 小图可直接上传。
7. 工程文件分类仍按原逻辑上传，不做压缩。
8. 错误提示：
   - 读取失败。
   - 压缩失败但已 fallback。
   - 上传失败。
   - 权限不足。

### 验收标准

- 移动端图纸上传有拍照入口。
- 移动端工序照片上传有拍照入口。
- 拍照后可预览。
- 压缩后可上传。
- 压缩失败可回退上传原图。
- 相册/文件选择仍可用。
- 工程文件上传不受影响。

## P5. 前端性能小优化

### 目标

用低风险方式减少移动端和列表页加载压力，不做大规模前端重构。

### 范围

- 图片懒加载。
- 搜索 debounce。
- 减少重复请求。
- 不改 UI 大结构。

### 涉及文件

- `client/src/views/Orders.vue`
- `client/src/views/Customers.vue`
- `client/src/views/Inventory.vue`
- `client/src/components/DrawingPanel.vue`
- 可新增 `client/src/utils/debounce.js`

### 实施步骤

1. 为图片预览添加 `loading="lazy"`。
2. 搜索输入增加 debounce：
   - 订单搜索。
   - 客户搜索。
   - 库存搜索。
3. 列表筛选变化时避免重复触发请求。
4. 大图预览只在用户点击时加载原图。
5. 上传成功后只刷新必要数据，避免整页无意义重载。

### 验收标准

- 图片列表不会一次性强制加载所有图片。
- 搜索输入不会每个字符都立即发请求。
- 订单、客户、库存列表原功能保持不变。
- 移动端浏览图纸列表更顺滑。

## P6. 后端安全、健康检查与错误响应

### 目标

修复最基础的生产安全和运维可见性问题：健康检查、JWT 配置、CORS 配置、无效 token 行为、统一错误读取。

### 范围

- 不做数据库结构大改。
- 不做权限体系重写。
- 只修低风险基础问题。

### 涉及文件

- `server-python/main.py`
- `server-python/routers/auth.py`
- `server-python/routers/documents.py`
- `server-python/routers/users.py`
- `server-python/routers/notifications.py`
- `client/src/api/index.js`

### 实施步骤

1. 新增 `/healthz`：
   - 返回 `{ "ok": true }`。
   - 可选检查数据库连接。
2. `SECRET_KEY` 从环境变量读取。
3. `CORS_ORIGINS` 从环境变量读取：
   - dev 可放宽。
   - prod 必须限定域名。
4. 修复 `/auth/me`：
   - 没有 token 返回 `401`。
   - token 无效返回 `401`。
   - 不再 fallback 到用户 `1`。
5. 抽出认证依赖函数，逐步减少多处重复解析 JWT。
6. 前端错误处理优先读取 `detail`，兼容 `error`。
7. 审计日志调整为请求成功后记录，避免失败请求被记录为成功操作。

### 验收标准

- `/healthz` 可用。
- 无效 token 不会登录成用户 1。
- 正式环境 CORS 可通过环境变量限制。
- 前端错误提示不因 `detail/error` 不一致而丢失。
- 写操作失败时不记录为成功审计。

## P7. 后端查询性能优化

### 目标

先解决容易出现的 N+1 查询和列表接口性能问题，避免订单、客户、库存、图纸数据稍多后明显变慢。

### 范围

- 不改接口返回结构，避免影响前端。
- 不做分页体系大重构。
- 优先优化订单和图纸相关热点接口。

### 涉及文件

- `server-python/routers/orders.py`
- `server-python/routers/documents.py`
- `server-python/routers/inventory.py`
- `server-python/routers/dashboard.py`

### 实施步骤

1. 优化订单列表：
   - 批量加载 customer。
   - 批量加载 current step。
   - 避免每条订单单独查一次工序。
2. 优化订单详情：
   - 一次性加载订单、客户、文档、流程、工序。
3. 优化图纸列表：
   - 按 `order_id/category` 查询时配合索引。
   - 返回时避免额外查订单造成 N+1。
4. 优化 dashboard：
   - 保持简单聚合。
   - 必要时减少重复 count。
5. 对高风险查询加注释说明数据流。

### 验收标准

- 订单列表返回结构不变。
- 订单详情返回结构不变。
- 查询次数明显减少。
- 现有前端页面不需要同步大改。

## P8. Alembic 迁移与数据库索引

### 目标

建立正式的数据库变更机制，补齐常用查询索引和关键唯一约束，逐步替代启动时 `ALTER TABLE` 的做法。

### 范围

- 引入 Alembic baseline。
- 添加索引和约束。
- 不做复杂历史数据清洗，除非唯一约束发现重复数据。

### 涉及文件

- `server-python/alembic.ini`
- `server-python/alembic/`
- `server-python/models.py`
- `server-python/main.py`
- `docs/reference-database.md`

### 实施步骤

1. 初始化 Alembic。
2. 建立 baseline migration。
3. 添加索引：
   - `orders(status, created_at)`
   - `orders(customer_id)`
   - `orders(priority)`
   - `process_flows(order_id, is_template)`
   - `process_steps(flow_id, seq)`
   - `documents(order_id, category, status, created_at)`
   - `documents(step_id)`
   - `inventory_reservations(order_id)`
   - `inventory_reservations(item_id)`
   - `notifications(to_user_id, is_read, created_at)`
   - `audit_logs(created_at, user_id, entity_type)`
4. 添加约束：
   - `page_permissions(user_id, page_key)` unique。
   - 可评估 `inventory_reservations(order_id, item_id)` unique。
5. SQLAlchemy engine 增加：
   - `pool_pre_ping=True`
   - `pool_size=5`
   - `max_overflow=10`
6. 逐步减少 `main.py` 中直接 `ALTER TABLE`。

### 验收标准

- 新环境可以通过 Alembic 创建结构。
- 老环境迁移可执行。
- 常用列表和详情查询有对应索引。
- 数据库文档和实际模型/迁移一致。

## P9. 数据库与 uploads 备份恢复

### 目标

建立可执行、可恢复的轻量备份方案。备份必须同时包含数据库和上传文件。

### 范围

- 添加备份脚本。
- 添加恢复脚本。
- 添加文档。
- 不引入复杂备份平台。

### 涉及文件

- `ops/backup/backup.sh`
- `ops/backup/restore.sh`
- `docs/backup-restore.md`
- `docker-compose.prod.yml`
- `.env.prod.example`

### 实施步骤

1. 新增 `backup.sh`：
   - 使用 `pg_dump -Fc` 备份 PostgreSQL。
   - 压缩 `uploads/`。
   - 生成日期目录。
   - 写入备份元信息。
2. 新增 `restore.sh`：
   - 恢复数据库 dump。
   - 恢复 uploads。
   - 支持恢复到临时库用于演练。
3. 加入保留策略：
   - 本机 7 份日备。
   - 异地 4 份周备。
4. VPS 方案：
   - cron 每天凌晨备份。
   - rclone 同步到对象存储、NAS 或另一台机器。
5. 办公室/NAS 方案：
   - 本机备份到独立磁盘。
   - 再同步一份到云端或 VPS。
6. 文档写清 RPO/RTO：
   - RPO 24 小时。
   - RTO 1-2 小时。

### 验收标准

- 能生成数据库 dump。
- 能生成 uploads 压缩包。
- 能恢复到临时数据库。
- 能恢复 uploads 到临时目录。
- 每月恢复演练流程清晰。

## P10. GitHub Actions 自动化部署

### 目标

建立简单可靠的自动化部署流程：代码通过测试后，SSH 到 VPS 或办公室/NAS 主机部署正式 compose。

### 范围

- 新增 CI/CD workflow。
- 使用 SSH 远程执行部署命令。
- 不引入镜像仓库。

### 涉及文件

- `.github/workflows/deploy.yml`
- `docs/deployment.md`
- `.env.prod.example`

### 实施步骤

1. 新增 workflow 触发条件：
   - push 到 `V2`。
   - 手动 `workflow_dispatch`。
2. CI 步骤：
   - 前端安装依赖并 build。
   - 后端安装依赖并运行 pytest。
3. 部署步骤：
   - SSH 到目标服务器。
   - 进入 `APP_DIR`。
   - `git pull`。
   - 执行 `docker compose -f docker-compose.prod.yml --env-file .env.prod up -d --build`。
4. GitHub Secrets：
   - `SSH_HOST`
   - `SSH_USER`
   - `SSH_KEY`
   - `APP_DIR`
   - 可选 `DEPLOY_DOMAIN`
5. 文档分别说明 VPS 和办公室/NAS 如何配置 Secrets。

### 验收标准

- 手动触发 workflow 能成功构建。
- 测试失败时不会部署。
- 测试成功后能远程部署。
- VPS 和办公室/NAS 只需替换 Secrets。

## P11. 测试体系补齐

### 目标

建立项目最低可用测试体系，覆盖登录、上传、工序照片、库存扣减、权限、前端上传交互和 E2E 主链路。

### 范围

- 新增测试框架和基础测试。
- 不追求一次性覆盖所有页面。
- 优先覆盖高风险路径。

### 涉及文件

- `server-python/tests/`
- `client/src/**/*.test.js`
- `playwright.config.*`
- `tests/e2e/`
- `docs/testing.md`
- `package.json`
- `requirements.txt`

### 实施步骤

1. 后端 pytest：
   - 登录成功/失败。
   - 无效 token 返回 `401`。
   - 只读用户不能上传或删除图纸。
   - 图纸上传格式校验。
   - 超过 50MB 上传失败。
   - photo 工序无照片不能完成。
   - photo 工序上传照片后可以完成。
   - 库存预留、扣减、撤回恢复正确。
2. 前端 Vitest：
   - 图片压缩 helper 成功路径。
   - 压缩失败 fallback。
   - 非图片不压缩。
   - 小图不无意义放大。
   - 上传弹窗权限控制。
   - 移动端拍照入口显示。
   - 上传预览显示。
3. E2E Playwright：
   - 登录 admin。
   - 打开订单详情。
   - 上传图片。
   - 完成需要照片的工序。
   - 查看图纸/照片预览。
   - 移动端 viewport 验证拍照入口。
4. 文档写清测试命令。

### 验收标准

- 后端测试可独立运行。
- 前端测试可独立运行。
- E2E 能跑通主链路。
- CI 能执行测试。

## P12. 文档与一致性整理

### 目标

让文档和实际代码保持一致，降低后续维护和交接成本。

### 范围

- 修正文档错误。
- 补充开发、部署、备份、测试文档。
- 不改业务逻辑。

### 涉及文件

- `README.md`
- `DOCKER_GUIDE.md`
- `docs/reference-api.md`
- `docs/reference-database.md`
- `docs/deployment.md`
- `docs/backup-restore.md`
- `docs/testing.md`

### 实施步骤

1. 修正 API 文档：
   - 图纸上传实际是 `POST /documents`。
   - 通知全部已读实际是 `PUT /notifications/read-all`。
2. 修正数据库文档：
   - 以 `models.py` 和 Alembic migration 为准。
   - 移除不存在字段或标明兼容字段。
3. `README.md` 保留项目介绍和快速开始。
4. `DOCKER_GUIDE.md` 写清：
   - 开发环境命令。
   - 正式环境命令。
   - 重建、日志、播种、重置数据。
5. `docs/deployment.md` 写清：
   - VPS 部署。
   - 办公室/NAS 部署。
   - 域名 HTTPS。
   - IP+端口 fallback。
   - 端口开放策略。
6. `docs/backup-restore.md` 写清：
   - 备份。
   - 恢复。
   - 恢复演练。
7. `docs/testing.md` 写清：
   - 后端测试。
   - 前端测试。
   - E2E 测试。
   - CI 执行方式。

### 验收标准

- 文档中的命令能执行。
- 文档中的接口和代码一致。
- 新人可以根据文档启动开发环境。
- 运维人员可以根据文档部署正式环境和恢复备份。

## 覆盖矩阵

| 原总计划内容 | 对应小计划 | 是否覆盖 |
| --- | --- | --- |
| 开发/正式环境必须拆分 | P1, P2, P3 | 已覆盖 |
| `docker-compose.dev.yml` | P2 | 已覆盖 |
| `docker-compose.prod.yml` | P3 | 已覆盖 |
| 开发/正式 Dockerfile 拆分 | P2, P3 | 已覆盖 |
| `.env.example`、`.env.dev`、`.env.prod.example`、`.env.prod` | P1 | 已覆盖 |
| Caddy/Nginx 反向代理 | P3 | 已覆盖 |
| 域名 HTTPS | P3, P12 | 已覆盖 |
| IP+端口 fallback | P3, P12 | 已覆盖 |
| VPS 部署 | P3, P9, P10, P12 | 已覆盖 |
| 办公室/NAS 部署 | P3, P9, P10, P12 | 已覆盖 |
| 移动端拍照上传 | P4 | 已覆盖 |
| 上传预览 | P4 | 已覆盖 |
| 前端图片压缩 | P4 | 已覆盖 |
| 保留相册/文件选择 | P4 | 已覆盖 |
| 工程文件不压缩 | P4 | 已覆盖 |
| 前端静态构建 | P3 | 已覆盖 |
| 图片懒加载 | P5 | 已覆盖 |
| 搜索 debounce | P5 | 已覆盖 |
| `/healthz` | P6 | 已覆盖 |
| `SECRET_KEY` 环境变量 | P1, P6 | 已覆盖 |
| CORS 环境变量 | P1, P6 | 已覆盖 |
| `/auth/me` 无效 token 修复 | P6 | 已覆盖 |
| 统一错误响应读取 | P6 | 已覆盖 |
| 审计日志成功后记录 | P6 | 已覆盖 |
| 订单 N+1 查询优化 | P7 | 已覆盖 |
| 图纸/订单/库存查询优化 | P7 | 已覆盖 |
| Alembic baseline | P8 | 已覆盖 |
| 数据库索引 | P8 | 已覆盖 |
| 数据库连接池配置 | P8 | 已覆盖 |
| `pg_dump -Fc` | P9 | 已覆盖 |
| `uploads/` 备份 | P9 | 已覆盖 |
| `backup.sh` / `restore.sh` | P9 | 已覆盖 |
| 7 天日备 / 4 周周备 | P9 | 已覆盖 |
| rclone 异地同步 | P9 | 已覆盖 |
| GitHub Actions | P10 | 已覆盖 |
| SSH 部署 | P10 | 已覆盖 |
| 不引入镜像仓库 | P10 | 已覆盖 |
| 后端 pytest | P11 | 已覆盖 |
| 前端 Vitest | P11 | 已覆盖 |
| Playwright E2E | P11 | 已覆盖 |
| API 文档修正 | P12 | 已覆盖 |
| 数据库文档修正 | P12 | 已覆盖 |
| 部署文档 | P12 | 已覆盖 |
| 备份恢复文档 | P12 | 已覆盖 |
| 测试文档 | P12 | 已覆盖 |
| 不引入 Kubernetes/Redis/CDN/对象存储直传/微服务 | 全部计划约束 | 已覆盖 |

## 建议批次

如果希望进一步压缩实施节奏，可以按批次做：

1. 第一批：P1 + P2  
   先把开发环境跑稳，后续改造不影响日常开发。
2. 第二批：P4 + P5  
   先解决移动端拍照上传这个直接业务痛点。
3. 第三批：P6 + P7  
   修安全和性能基础问题。
4. 第四批：P3 + P8  
   建立正式环境和数据库迁移基础。
5. 第五批：P9 + P10  
   补备份和自动化部署。
6. 第六批：P11 + P12  
   补测试和文档，使项目可持续维护。
