# 汇易通热流道管理系统 - V2.4 版本更新总结

V2.4 版本是一个聚焦于**全站列表性能大普调、高光瞬间定位与全局 CSS 动画重构、SSE 实时推送系统、用户账密字段精简**以及**UI细节体验深度打磨**的里程碑版本。

在本次更新中，我们彻底解决了一直以来因为“高亮定位延迟、列表渲染卡顿、不同账号左/右上角显示不统一”的系统痛点，完成了全栈的核心列表性能升级与高亮跳转体验。

---

## 1. 🔔 实时推送与 0 延迟秒显高亮定位机制 (Real-time SSE & 0-Delay Highlight Locating)
我们重新梳理并重构了通知消息到页面定位高亮的完整闭环链路，彻底解决之前“点击通知跳转定位慢、高亮慢、无闪烁效果”等技术瓶颈：

### 1) 后端 SSE (Server-Sent Events) 实时消息通道
- **全双工事件流**：在后端 `routers/notifications.py` 新增了 `ConnectionManager` 实时通道，通过 `/notifications/stream` SSE 接口实现了后端向前端的即时数据推送。
- **低损耗心跳检测**：内置了 45 秒超长间隔心跳机制（`ping`），在维持与浏览器稳定连接的同时，将服务器运行与唤醒损耗降到了最低。
- **无感刷新机制**：每当有新通知产生，SSE 通道将直接唤醒前端刷新状态，无需浏览器重复发起轮询请求。

### 2) 0 延迟“分流判定”滚动高亮逻辑
在前端（如 `Orders.vue`、`Inventory.vue`）点击通知跳转时，升级为**两阶段分流定位**：
- **第一阶段：内存就地检测（0 延迟秒显）**
  - 首先重置高亮 ID (`highlightedId = null`) 并执行 `nextTick()`，以此**强行重置 DOM，确保下一次高亮闪烁动画能百分之百重放**。
  - 检测当前已驻留的列表数据中是否存在目标行。
  - **如果存在**：直接触发滚动定位 `scrollIntoView` 并开始高亮闪烁，实现 **0 延时瞬间视觉反馈**。随后，静默（在后台）调用分页定位接口，确保前端分页器状态最终一致。
- **第二阶段：后端接口定位分页**
  - **如果不存在**：前端向后端新增的专属定位路由 `/orders/{id}/locate` 或 `/inventory/{id}/locate` 发起轻量请求。
  - **高效计算**：后端直接在数据库中按照当前的排序、搜索条件计算出目标记录在第几页，并仅返回一个 `page` 索引号。前端拿到后立刻跳到对应页码并渲染，再执行平滑滚动与高亮。这规避了原本需要“拉取十万条大列表在前端肉眼寻找”的毁灭性性能危机。

### 3) 突破 Scoped CSS 局限的全局动画规范
- 之前高亮偶尔失效或“仅剩一条蓝色边框”的元凶是 **Vue Scoped CSS 的哈希穿透限制**，以及 Element Plus 自身变量对 `background-color` 的强制覆盖。
- **方案解决**：我们将 `@keyframes custom-flash` 及具体的 td 单元格动画控制，移入了全局的 `index.css`。
- **双端闪烁统一**：
  - **桌面端**：直接穿透 Element 内部的 `tr.highlight-flash-row td.el-table__cell` 触发多次闪烁。
  - **移动端**：为卡片样式追加 `.highlight-flash-card-active`，使得在手机屏幕上点击跳转时，对应的工序或订单卡片也会呈现柔和且明显的蓝色呼吸灯闪烁。

---

## 2. ⚡ 全站核心列表 V2 引擎全面铺开 (KeepAlive & Silent Refresh)
为了解决原先各个列表页面在切换时需要转圈（v-loading）等待的问题，我们将验证通过的 V2 高性能架构全面推广：
- **四大核心页面升级**：**客户管理** (`Customers.vue`)、**工艺模板** (`ProcessFlow.vue`)、**外协管理** (`Outsourcing.vue`)、**用户管理** (`Users.vue`) 均重构为了 V2 引擎。
- **驻留内存与 0 延迟瞬间加载**：这些页面已全部加入 `KeepAlive` 缓存。在菜单间来回切换时，原本的数据和滚动条位置都会被瞬间保留，杜绝了反复销毁和挂载组件造成的系统卡顿。
- **无感刷新（静默加载）机制**：在用户切回页面的一瞬间，系统直接从内存中展示旧数据，并静默（在后台）向后端发起请求获取最新数据。只有在静默刷新完成后，页面内容才会无感更新，去除了不友好的全局转圈等待效果。

---

## 🚀 3. 客户详情页性能提速与状态路由动态监听 (CustomerDetail KeepAlive & Watch)
客户详情页内部同样包含了庞大的订单关联数据列表，为避免频繁进出该页面时出现漫长的加载期：
- **详情页加入 KeepAlive 缓存**：在主框架中把 `CustomerDetail` 纳入缓存队列，极大地提升了“客户列表 <-> 客户详情”反复横跳时的加载速度。
- **动态 ID 路由守卫监听**：由于详情页绑定了动态的客户 ID 路由参数 (`/customers/:id`)，为防止缓存导致的数据“错乱/不更新”，我们引入了 `onActivated` 和 `watch` 机制：
  - 如果检测到用户点进了**不同的客户**，页面会精准触发重新加载；
  - 如果用户只是在详情页和其他页面来回切换，页面会在**0 延迟秒显**的基础上，触发**静默刷新拉取**最新的订单列表，实现高性能与高准确度的完美平衡。

---

## 🧹 4. 用户表字段大瘦身与数据库冗余清理 (User Field Slimming & DB Columns Dropped)
为了彻底解决不同账号在左下角和右上角显示格式不一的问题，我们清除了过度设计的名称和角色体系，回归极简结构：
- **数据库冗余物理删除**：直接执行 PostgreSQL 的 `DROP COLUMN` 操作，从 `users` 数据表中删除了 `display_name`（显示名称）和 `role_label`（角色标签）两个冗余字段。
- **后端服务全面剥离**：
  - 删除了 SQLAlchemy 模型层 (`models.py`) 和 Pydantic 数据规范层 (`schemas.py`) 中这两个字段的全部绑定与验证定义。
  - 修改了 `/users` 相关路由（新建与修改用户）和系统设置中操作日志记录 (`routers/settings.py`) 的对应逻辑。
  - 重构了初始化数据库脚本 `seed.py`，保证所有测试用户不再包含这两个字段。
- **前端输入与展示去冗余**：修改了用户管理 (`Users.vue`) 的表格，删除了展示该两个属性的列，并在新建/编辑弹窗中直接去除了这两项表单输入，只保留最核心的“用户名”、“密码”、“管理员状态”、“启用状态”。

---

## 👥 5. 侧边栏与右上角账号信息极简统一 (Sidebar & Header UI Uniformity)
对全站的用户登录状态展示进行了美学上的微调：
- **右上角统一**：移除原本读取 `display_name` 的代码，统一强制采用登录用户名 `username` 进行展示，再也不会有 `admin` 账号右上角显示空白的尴尬情况。
- **左下角侧边栏极简**：
  - 移除了原有的多余角色徽章（如“车间工人”、“设计师”等字段在数据库中删除后留下的空块）。
  - 移除了原本硬编码的“用户”两个字，仅留下一行简洁、温馨的问候语：**`用户名，XX好`**（例如：`admin，下午好`、`laowang，晚上好`），视觉上大方得体。
- **网络连接防阻塞**：修复了在开发阶段修改 `models.py` 触发后端容器热重启时，因 Server-Sent Events (SSE) 强连接未释放导致后端容器挂起、引发前端短暂白屏和登不上的连接问题（通过对 backend 容器进行安全重启彻底解决）。

---

## 🎨 6. 订单工序流转状态标签 UI 统一 (OrderDetail Tag Styles Alignment)
在订单详情页 (`OrderDetail.vue`) 中，原先“有无编辑权限”看到的工序列表状态标签风格大不相同，视觉极不统一。现已完成精细规范：
- **消除纯文字粗糙感**：当用户为“无操作/修改权限”角色时，原本的进度状态（已完成、进行中、跳过、未开始）只是简单的有色字。
- **标签框化改造**：将其重新设计为带有淡雅背景色、字色和虚化边框的 **精致边框圆角标签（Badge Tag）**。这不仅让无权限状态的 UI 呈现更加丰满高档，同时也与有权限操作时的“撤回”、“完成工序”等按钮在块级视觉尺寸上达到了 100% 对齐。

---

## 🛠️ 下一步开发与系统维护建议 (Developer Notes)
在当前 V2.4 纯净架构下，系统的用户体系已回归最基础的 `username` 认证。后续若需重新引入复杂的岗位（角色）管理，建议在后端使用标准的角色权限表（如角色-权限多对多关联）进行设计，避免再次在 `users` 表中硬编码非规范的角色文字。
