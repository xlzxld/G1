# 汇易通热流道管理系统 V2.5 更新点总结

本报告总结了在 V2.5 阶段由 AI 驱动完成的所有架构优化、性能提升及缺陷修复工作。整体工程已完全覆盖原定的 P1、P2、P3、P5、P6、P7、P8 实施计划，并在一系列边缘测试中加固了系统的数据一致性。

---

## 1. 架构级重构与解耦 (P1)
系统已完全淘汰了早期硬编码凭证的开发模式，全面拥抱环境隔离：
- **配置文件分层**：剥离出了 `.env.example`, `.env.prod.example` 以及本地专用的 `.env.dev`。
- **动态依赖注入**：将诸如 `DATABASE_URL`、JWT 的 `SECRET_KEY`、上传目录 `UPLOAD_DIR` 以及 `CORS_ORIGINS` 白名单等系统命脉，全部通过操作系统级别的环境变量安全下发，彻底杜绝凭证入库泄露的安全隐患。

## 2. 数据库性能、迁移与一致性增强 (P8 & 数据一致性修复)
- **迁移正规化**：彻底告别 `Base.metadata.create_all` 这种开发阶段的粗暴建表模式，全面启用 Alembic 生成了 `0001_initial_baseline` 版本基线，为后续在正式环境平滑升降级表结构奠定了正规流程基石。
- **高频字段索引化**：针对外键（如 `customer_id`）、状态（`status`）、优先级和创建时间等关键查询列加入了 B-Tree 物理索引（`index=True`）。
- **孤儿数据与幽灵缓存修复**：
  - **外键约束容错**：保障客户被删除时触发 `ondelete="SET NULL"`，订单不被级联删除而成为孤儿订单。
  - **冗余字段同步**：修复了订单分配给新客户后，底层冗余的 `customer_name` 字段未能同步更新导致的“搜索错乱”问题，并清洗了现存库表错误。

## 3. 后端 API 性能极限优化 (P7)
- **消灭 N+1 查询风暴**：重写了整个 `get_orders` 分页列表接口。过去对于列表中每一条订单都要去数据库二次请求它所属的“客户详情”和“当前最新工序”，这使得查询消耗随数据量呈倍数剧增。
- **内存批量装配**：引入了 SQLAlchemy 的预加载策略 `joinedload`，并通过提取页面所有 `step_ids` 实施 `in_()` 批量查询。将上百条 SQL 请求缩减为 2 次聚合查询，在内存中实施高速拼装，使千级数据的翻页达到毫秒级响应。

## 4. 系统级安全漏洞封堵 (P6)
- **零授权越权修复**：封堵了之前在 `/auth/me` 中，当客户端携带空 Token 或非法请求头时，系统会错误降级默认返回 `User ID = 1`（即最高权限管理员）的致命越权漏洞。目前该接口采用强认证依赖，未获权访问将直接拒绝请求。
- **运维探测针**：追加了专用于监控重启、自动漂移和存活检查的轻量级 API `/healthz` 端点。
- **审计日志降噪**：重构了 `audit_log_middleware` 的记录策略，仅对有效的（HTTP < 400）操作日志实施落盘，过滤了大量由于鉴权阻断带来的垃圾审计污染。

## 5. 前端交互体验跃升 (P5)
- **防抖式全局搜索**：自主封装并启用了 `debounce.js`。在订单、库存、客户等所有涉及高频输入检索的场景，系统已实现无需回车、且不乱发无效请求的 300 毫秒延时无感检索体验。
- **解决 Vue 切换幽灵状态**：排查并彻底修复了 SPA 单页应用中 `<keep-alive>` 特性带来的重灾区问题。通过引入 `onActivated` 生命周期钩子，在页面切回激活时静默静默刷新客户与模板列表，避免在其他 Tab 删除业务对象后，当前下拉框仍出现已作废的“幽灵选项”。
- **长列表媒体懒加载**：对于海量工程图库和工序抓拍照片引入了 `loading="lazy"` 策略，防止并发请求拉爆浏览器显存。

## 6. 环境隔离与生产架构升级 (P2 & P3)
为了保障应用在公网的稳定安全，系统实现了开发与生产环境的彻底解耦：
- **容器镜像重构**：剥离出了 `Dockerfile.dev`（带有 Vite 与 Uvicorn 热更新支持）与 `Dockerfile.prod`（Node 静态预编译 + 无热更的高并发后端）。
- **组合编排解耦**：移除了混杂的旧版编排，建立了专用的 `docker-compose.dev.yml` 与 `docker-compose.prod.yml`。生产环境不再将应用的 8000 和 5173 端口暴露至公网。
- **Caddy 反向代理接入**：新增 `Caddyfile.prod`。生产环境引入了 Caddy 作为统一入口（80/443），承接所有静态文件的下发，并通过无缝剥离前缀的反向代理策略将接口与上传请求静默转发至后端微服务。

---
**实施时间**：2026年7月
**完成状态**：✅ 全部功能已闭环验证并可通过容器环境直接拉起测试
