# 汇易通 MES V2 轻量优化、环境拆分与公网部署计划

## Summary

本计划只做规划，不实施。目标是在保持“小型项目、容易落地”的前提下，把现有 FastAPI + Vue + PostgreSQL + Docker Compose 系统整理成可开发、可部署、可备份、可公网访问、可移动端直拍上传的稳定形态。

优先顺序：

1. 移动端图纸/照片直拍上传：相机入口、预览、前端压缩、保留相册/文件选择。
2. 环境配置拆分：开发环境和正式环境必须分开，尤其是 Docker、Dockerfile、环境变量、启动命令、反向代理配置和部署文档。
3. 生产部署基础：前端静态构建，后端关闭 reload，数据库不暴露公网，反向代理统一入口。
4. 性能与稳定性：修复明显 N+1 查询，补索引，加健康检查，锁定 CORS/JWT 配置。
5. 数据库备份容灾：PostgreSQL 和 uploads 文件一起备份，支持恢复演练。
6. 自动化部署：GitHub Actions 做构建、测试、SSH 部署。
7. 测试体系：补最小但有效的后端、前端和 E2E 测试。

默认规模假设：小团队内部使用，几十个用户以内，订单、库存、图纸和照片数据量中小，单机 Docker Compose 足够。

## Key Changes

### 1. 环境配置拆分

开发环境和正式环境应明确拆分，避免把热更新、源码挂载、局域网 IP、数据库公网端口、reload 等开发配置带到正式环境。

建议新增或调整：

- `docker-compose.dev.yml`：开发环境。
- `docker-compose.prod.yml`：正式环境。
- `client/Dockerfile.dev`：Vite dev server，支持热更新。
- `client/Dockerfile.prod`：构建静态文件，由 Caddy/Nginx 提供服务。
- `server-python/Dockerfile.dev`：`uvicorn --reload`，挂载源码。
- `server-python/Dockerfile.prod`：无 `--reload`，正式启动。
- `.env.example`：通用环境变量模板，提交到仓库。
- `.env.dev`：本地开发配置，可不提交真实密码。
- `.env.prod.example`：正式环境模板，提交到仓库。
- `.env.prod`：正式服务器真实配置，不提交 Git。
- `Caddyfile.prod` 或 `nginx.prod.conf`：正式反向代理配置。
- `docs/deployment.md`：正式部署、公网访问、备份恢复文档。
- `DOCKER_GUIDE.md`：补充开发/正式两套命令。

环境变量至少应覆盖：

- `DATABASE_URL`
- `POSTGRES_USER`
- `POSTGRES_PASSWORD`
- `POSTGRES_DB`
- `SECRET_KEY`
- `CORS_ORIGINS`
- `APP_ENV`
- `UPLOAD_DIR`
- `BACKUP_DIR`
- `PUBLIC_BASE_URL`

### 2. 开发环境 Docker 方案

`docker-compose.dev.yml` 保持当前开发体验：

- 前端暴露 `9956:5173`，运行 Vite dev server。
- 后端暴露 `8080:8000`，运行 `uvicorn --reload`。
- PostgreSQL 可映射 `5432:5432`，方便本地数据库工具连接。
- `client/` 和 `server-python/` 使用 bind mount，方便热更新。
- 前端 API 统一使用相对路径 `/api`，由 Vite proxy 转发到 `backend:8000`。
- 不在前端或 compose 中硬编码局域网 IP。

开发命令：

```bash
docker compose -f docker-compose.dev.yml up -d
docker compose -f docker-compose.dev.yml exec backend python seed.py
docker compose -f docker-compose.dev.yml exec backend python seed_mock.py
```

### 3. 正式环境 Docker 方案

`docker-compose.prod.yml` 改成生产形态：

- 前端不运行 Vite dev server，改为 `npm run build` 后提供静态文件。
- 后端不使用 `--reload`。
- 后端建议命令：`uvicorn main:app --host 0.0.0.0 --port 8000 --workers 2`。
- PostgreSQL 不映射公网端口，只在 Docker 网络内访问。
- `uploads/` 使用命名卷或宿主机目录持久化。
- `backups/` 使用宿主机目录持久化。
- 所有服务设置 `restart: unless-stopped`。
- 增加 healthcheck：数据库、后端 `/healthz`、前端反向代理入口。
- 只开放 `80/443` 给公网访问，`22` 给 SSH 运维。
- 不开放 `5432`、`8000`、`5173` 到公网。

正式启动命令：

```bash
docker compose -f docker-compose.prod.yml --env-file .env.prod up -d --build
```

### 4. 反向代理与公网访问

正式环境建议增加 Caddy 或 Nginx。小项目优先 Caddy，因为自动 HTTPS 更省事。

域名 HTTPS 方案：

- `https://your-domain.com` 访问前端。
- `/api/*` 反向代理到后端容器。
- `/uploads/*` 提供上传文件访问。
- Caddy 自动签发和续期 TLS 证书。

IP+端口方案：

- 作为临时访问或无域名 fallback。
- 可用 `http://服务器IP` 或指定端口访问。
- 长期正式使用仍推荐域名 + HTTPS。
- 如果使用移动端拍照上传，HTTPS 更稳妥，浏览器权限和安全策略更少出问题。

VPS 方案：

- 云防火墙只开放 `22/80/443`。
- 服务器部署目录放 `.env.prod`，不提交到 Git。
- 使用正式 compose 启动。
- 每天执行备份，并同步一份到异地。

办公室/NAS 方案：

- 有公网 IP：路由器转发 `80/443` 到部署主机。
- 无固定 IP：使用 DDNS。
- 无公网 IP：再考虑 Cloudflare Tunnel 或其他内网穿透。
- 需要明确风险：断电、宽带变动、运营商封端口会影响公网可用性。
- 建议配 UPS，并把备份同步到 VPS 或云端。

### 5. 移动端直拍上传

在图纸上传和工序照片上传中增加移动端相机入口。

涉及位置：

- 图纸/附件上传组件。
- 订单详情里的工序照片上传弹窗。

交互设计：

- “拍照上传”：调用移动端相机。
- “从相册/文件选择”：保留现有选择能力。
- 拍照或选图后先显示预览。
- 用户确认后再上传。
- 工程文件仍走普通文件选择，不做图片压缩。

技术方案：

- 使用原生 `<input type="file" accept="image/*" capture="environment">`。
- `capture="environment"` 优先打开后置摄像头。
- 前端用 Canvas 压缩图片：
  - 长边默认压缩到 `1920px`。
  - JPEG 质量默认 `0.82`。
  - 保留原文件名语义，生成新的压缩 `File`。
  - 压缩失败时回退上传原图。
- 后端 `POST /documents` 接口保持不变，仍走 `multipart/form-data`。

安全建议：

- 正式环境建议禁用或严格限制 SVG 预览上传，避免 SVG 作为可执行内容带来 XSS 风险。
- 后端继续保留扩展名和 MIME 校验。
- 后续如照片量变大，再考虑生成缩略图，不在本阶段引入。

### 6. 前端性能优化

- 正式环境使用静态构建，不运行 Vite dev server。
- 路由懒加载和现有 `keep-alive` 保留。
- 搜索输入加 debounce，减少订单、客户、库存列表的重复请求。
- 图片列表加 `loading="lazy"`。
- 大图预览避免一次性加载全部原图。
- 移动端上传优先使用压缩后的图片，降低上传耗时和流量。
- 前端 API 统一使用相对路径 `/api`，生产由反向代理转发，避免硬编码局域网 IP。

### 7. 后端性能与安全优化

- 增加 `/healthz` 健康检查接口，供 Docker healthcheck、反向代理和部署脚本使用。
- 将 `SECRET_KEY`、数据库密码、CORS 域名放入环境变量。
- 修复 `/auth/me` 无效 token fallback 到用户 `1` 的行为，无有效 token 直接返回 `401`。
- 生产环境关闭 reload。
- 订单列表和详情避免 N+1 查询：
  - 使用 `selectinload` / `joinedload`。
  - 或显式批量查询当前工序、客户、文档后组装返回。
- 审计日志建议在请求成功后记录，避免失败请求也被记录为成功操作。
- 统一错误响应字段，前端优先读取 `detail`，避免有的地方读 `error` 有的地方读 `detail`。

### 8. 数据库性能优化

引入 Alembic 管理迁移：

- 先做 baseline migration。
- 后续字段、索引、约束变更都走迁移。
- 逐步减少 `main.py` 启动时直接 `ALTER TABLE` 的做法。

建议补充索引：

- `orders(status, created_at)`
- `orders(customer_id)`
- `orders(priority)`
- `process_flows(order_id, is_template)`
- `process_steps(flow_id, seq)`
- `documents(order_id, category, status, created_at)`
- `documents(step_id)`
- `inventory_reservations(order_id)`
- `inventory_reservations(item_id)`
- `inventory_reservations(order_id, item_id)` 唯一约束可考虑，用于防止同一订单同一物料重复记录。
- `notifications(to_user_id, is_read, created_at)`
- `audit_logs(created_at, user_id, entity_type)`
- `page_permissions(user_id, page_key)` 唯一约束。

SQLAlchemy engine 建议配置：

- `pool_pre_ping=True`
- `pool_size=5`
- `max_overflow=10`

### 9. 数据库备份与容灾

备份必须包含两部分：

- PostgreSQL：使用 `pg_dump -Fc`。
- 上传文件：备份 `uploads/`。

建议新增：

- `ops/backup/backup.sh`
- `ops/backup/restore.sh`

备份策略：

- 本机保留 7 份日备。
- 异地保留 4 份周备。
- 每次备份生成日期目录，包含数据库 dump、uploads 压缩包、备份元信息。

VPS：

- cron 每天凌晨执行备份。
- 使用 `rclone` 同步到对象存储、NAS 或另一台机器。

办公室/NAS：

- 本机备份到独立磁盘。
- 再同步一份到云端或 VPS。
- 建议配 UPS，降低断电损坏风险。

恢复目标：

- RPO：24 小时。
- RTO：1-2 小时。
- 每月做一次恢复演练到临时数据库和临时 uploads 目录。

### 10. 自动化部署

新增 GitHub Actions：

- push 到 `V2` 或手动 `workflow_dispatch` 触发。
- 前端执行 install/build。
- 后端执行 pytest。
- 成功后通过 SSH 到目标机器部署。

部署方式保持简单：

- 服务器上保留 Git 仓库。
- CI 执行 `git pull`。
- 执行正式 compose 启动命令。

示例部署命令：

```bash
docker compose -f docker-compose.prod.yml --env-file .env.prod up -d --build
```

同一套 workflow 可用于：

- 单台 VPS。
- 办公室/NAS 主机。

GitHub Secrets：

- `SSH_HOST`
- `SSH_USER`
- `SSH_KEY`
- `APP_DIR`
- 可选：`DEPLOY_DOMAIN`

后续如果规模变大，再升级到 GHCR 镜像发布。本阶段不引入镜像仓库，减少复杂度。

### 11. 测试体系

后端建议新增 pytest：

- 登录成功/失败。
- 无效 token 返回 `401`。
- 图纸上传：允许图片/PDF/CAD，拒绝非法扩展。
- 超过 50MB 返回错误。
- `completion_condition=photo` 时无照片不能完成工序。
- 上传照片后可以完成 photo 类型工序。
- 库存预留、订单完成扣减、撤回恢复库存。
- 只读用户不能上传或删除图纸。

前端建议新增 Vitest：

- 图片压缩 helper：大图压缩、小图跳过、非图片跳过、压缩失败 fallback。
- 上传弹窗：移动端显示拍照入口。
- 工程文件不进入拍照压缩流程。
- 权限控制：无 `drawings.can_edit` 时不显示上传/删除。
- 上传前预览正常显示。

E2E 建议新增 Playwright：

- 登录 admin。
- 打开订单详情。
- 上传一张模拟图片。
- 确认图片预览出现。
- 对 photo 工序上传照片并完成工序。
- 移动端 viewport 下验证“拍照上传”按钮存在。

### 12. 文档与一致性整理

当前项目文档和实际接口存在少量不同步，优化时应顺手修正：

- 图纸上传实际是 `POST /documents`，不是旧文档里的 `/documents/upload/{order_no}`。
- 通知全部已读实际是 `PUT /notifications/read-all`。
- 数据库文档里提到的部分字段和约束，应以模型和迁移为准同步更新。

建议文档结构：

- `README.md`：保留项目介绍和快速开始。
- `DOCKER_GUIDE.md`：开发/正式 Docker 命令。
- `docs/deployment.md`：公网部署、域名、IP、VPS、办公室/NAS。
- `docs/backup-restore.md`：备份与恢复。
- `docs/testing.md`：测试命令和测试范围。

## Test Plan

### 开发环境验证

- `docker compose -f docker-compose.dev.yml up -d` 可启动。
- 前端 `http://localhost:9956` 可访问。
- 后端 `http://localhost:8080/docs` 可访问。
- `seed.py` 和 `seed_mock.py` 可执行。
- 修改前后端源码后可热更新。

### 正式环境验证

- `docker compose -f docker-compose.prod.yml --env-file .env.prod up -d --build` 可启动。
- 前端通过反向代理访问正常。
- `https://域名/api/healthz` 返回 OK。
- `https://域名/uploads/...` 可访问已上传文件。
- 数据库端口 `5432` 不对公网开放。
- 后端 `8000` 和前端 Vite `5173` 不对公网开放。

### 移动端上传验证

- 移动端图纸上传弹窗有“拍照上传”入口。
- 移动端工序照片弹窗有“拍照上传”入口。
- 拍照后能看到预览。
- 压缩后能正常上传。
- 压缩失败时能回退上传原图。
- 相册/文件选择仍然可用。
- 工程文件上传不受拍照逻辑影响。

### 后端测试

- 登录成功/失败。
- 无效 token 返回 `401`。
- 只读用户不能上传或删除图纸。
- 图纸上传格式校验。
- 超过 50MB 上传失败。
- photo 工序无照片不能完成。
- photo 工序上传照片后可以完成。
- 库存预留、扣减、撤回恢复正确。

### 前端测试

- 图片压缩 helper 覆盖成功、失败、非图片、小图场景。
- 上传弹窗权限控制正确。
- 移动端拍照入口显示正确。
- 上传预览显示正确。
- API 错误时提示明确。

### E2E 测试

- 登录。
- 进入订单详情。
- 上传图片。
- 完成需要照片的工序。
- 查看图纸/照片预览。
- 移动端 viewport 下验证拍照入口。

### 备份恢复验证

- 执行一次 `backup.sh`。
- 验证生成数据库 dump 和 uploads 包。
- 恢复到临时数据库。
- 恢复到临时 uploads 目录。
- 登录系统抽查订单、图纸、照片是否一致。

## Assumptions

- 只规划，不实施。
- 开发环境和正式环境必须拆分配置。
- 小型项目优先单机 Docker Compose。
- VPS 和办公室/NAS 两种部署方案都保留。
- 正式环境推荐域名 + HTTPS，IP+端口只作为备用或临时方案。
- 移动端拍照上传采用“相机 + 预览 + 压缩 + 保留相册选择”。
- 后端上传接口暂不改协议，仍使用现有 `POST /documents`。
- 不引入 Kubernetes、Redis、CDN、对象存储直传、复杂队列或微服务。
- 自动化部署先采用 SSH 拉代码并本机 build，不引入镜像仓库。
- 后续数据量明显变大时，再考虑对象存储、缩略图服务、CDN 和更完整监控。
