# Hot Runner MES 系统 - V2.2 版本更新点详细总结

V2.2 版本是一个涵盖了**数据模型变更**、**后端引擎升级**、**新功能模块集成**、**前端组件解耦重构**以及**深度交互体验缺陷修复**的重大版本更新。以下是本次更新的详细内容总结。

---

## 1. ⚙️ 全新通知联动与条件警报引擎 (Rule-Based Notification & Alert Engine)
### 1.1 后端引擎实现 (`server-python/routers/notifications.py`)
- **动态规则库**：新增 `NotificationRule`（通知规则）数据模型，支持在系统后台实时进行规则的增删改查。
- **条件判定机制 (`trigger_notification_rules`)**：
  - 基于事件流触发，支持 `order_created`（订单创建）、`order_completed`（订单完成）、`design_completed`（设计图纸完成）以及 `inventory_alert`（库存不足预警）等核心事件。
  - 支持复杂比较操作符的条件过滤判定，如：
    - `eq`：等于判定（用于优先级判定 `priority`）。
    - `contains`：模糊包含判定（用于产品名称 `product_name` 或图纸标题 `drawing_title`）。
    - `lt` / `gt`：数值小于 / 大于判定（用于库存可用量 `available` 校验）。
  - **模板引擎**：支持在通知的标题与正文中通过占位符（如 `{order_no}`、`{product_name}`、`{available}` 等）自动渲染上下文属性值。
  - **多通道定向投递**：支持将通知投递至“所有人 (all)”、“特定角色 (role:admin)”或“特定员工ID列表 (user_ids:1,2,5)”。

### 1.2 前端可视化后台配置 (`Settings.vue`)
- 在“系统设置”页面中，为管理员提供了“通知规则配置”功能区。
- **智能表单交互**：配置规则时，系统支持从用户名录中多选指定接收人。切换事件时，前端能够联动清空并重新关联该事件专属的字段与逻辑关系，且根据条件字段自动锁死特定的逻辑符号，避免无效配置。
- **状态快捷切换**：提供规则一键启用 / 禁用滑块。

---

## 2. 📦 生产零配件用料分配与库存联动锁定/扣减 (Material Allocation & Inventory Transaction)
### 2.1 零配件分配 (`client/src/components/OrderMaterials.vue`)
- 在订单详情页中引入了全新的“用料分配与零配件管理”卡片。
- **可用库存校验**：录入用料时，前端能够展示该零配件的物理总库存、已预留锁定库存，并实时计算当前可用量。分配数量严禁超出当前可用量：
  $$\text{Available} = \text{Total} - \text{Reserved}$$
- **事务预留锁定**：用料分配成功后，系统在数据库中使用 `with_for_update()` 排他锁更新对应物料，扣减其可用库存，并增加其预留数（`reserved` 增加），以保障高并发下库存的一致性。

### 2.2 状态流转与库存扣减还原联动 (`server-python/routers/orders.py`)
- **自动完工机制优化**：重新修正了完工检测逻辑。现在，仅当工艺流程中的所有步骤（包括必填与非必填）都处于 `completed` 或 `skipped`（已跳过）状态时，订单才会触发自动完工，将状态变更为 `completed`。
- **完工联动物理扣减 (`deduct_order_inventory`)**：当订单被手动标记为“已完成”，或通过工序流程全部走完自动完工时，系统会**扣除分配用料的物理库存量**（`total` 与 `reserved` 同步扣减），并释放预留锁定，打通了生产与库存的实物联动。
- **撤回/状态回滚还原 (`rollback_order_inventory`)**：为防止误操作，当订单从“已完成”退回至“进行中”（如回滚了最后一个工序，或者手动降级订单状态）时，系统自动执行逆向库存补偿，**还原被扣除的物理库存并重新变回预留锁定状态**，保证库存全局事务一致性。

---

## 3. 📐 工程图纸分级与多版本生命周期控制 (Drawings & Document Versioning)
### 3.1 图纸管理大板解耦 (`client/src/components/DrawingPanel.vue`)
- 原订单详情页内的简易图纸上传列表被彻底剥离，重构为独立的 `DrawingPanel.vue` 组件，避免布局在极端高度下的堆叠和排版错乱。
- **图纸多维度分类检索**：支持对图纸划分二级分类（2D图、3D图、工艺文件、其他附件），并动态统计各分类的文件数量。
- **双视觉检索模式**：
  - **预览图模式**：提供宫格缩略图墙。鼠标悬停时可展示大图遮罩层，快速进行大图全屏预览、基本信息编辑与删除；对于带有备注的图纸，悬浮提示备注详情气泡。
  - **工程文件模式**：提供规整的表格明细清单，支持直接点击或下载大文件。

### 3.2 磁盘级物理归类与版本更新控制 (`server-python/routers/documents.py`)
- **规范路径分类存储**：图纸文件上传后，统一规范落盘存储在按订单与图纸类型划分的路径中：
  `uploads/{order_no}/{category}/v{timestamp}-{filename}`
- **版本自动递增逻辑**：上传新文件时，系统会在当前订单与当前分类下，自动检测最新的版本号并做递增（`v1` $\to$ `v2` $\to$ `v3`）。
- **废弃判定与生命周期转化**：当新图纸作为 `active` 状态成功上传时，系统会自动将该订单及该分类下的所有旧 active 图纸在数据库层批量修改为 `deprecated`（废弃）状态，确保车间工人随时看到的均是最新版生效图纸。
- **磁盘物理删除联动**：删除图纸记录时，系统会尝试联动将磁盘上的物理文件彻底抹除，避免服务器垃圾空间堆积。

---

## 4. 🚀 交互体验缺陷与全局异常防御优化
### 4.1 全局详情页 404 防白屏兜底重定向
- 在通知中心点击某些可能已被管理员物理删除的记录时，原有系统由于直接加载组件而在获取空数据时产生渲染挂起（导致完全白屏）。
- 重新在 `OrderDetail.vue` 和 `CustomerDetail.vue` 的 `fetch` 失败回调中植入了错误路由守卫。一但捕获接口返回的资源不存在异常，会自动触发 `ElMessage.error` 给予明确警示，并兜底重定向至相应的列表主页，杜绝了白屏发生的可能。

### 4.2 列表精准跳转定位三件套（行高亮、自动翻页、自动滚动）
针对之前反复出现的行定位功能失效的问题，进行了底层三大技术攻坚：
1. **分页机制修复（自动翻页）**：
   - 彻底移除了 `el-pagination` 组件上无效的 `@change` 事件绑定。
   - 重新绑定为正确的 `@current-change` 和 `@size-change` 监听，使分页查询系统在页码被 `locateOrderPage` 重新计算并赋予目标值后，能够真正触发网络请求去拉取该页数据。
2. **时序与渲染缓冲（自动滚动）**：
   - 在 `Orders.vue` 和 `Inventory.vue` 接口加载完毕后，使用 `await nextTick()` 协调 Vue 的虚拟 DOM 渲染树。
   - 使用定时器轮询检测直到 DOM 结构中切实渲染出高亮节点后，延迟 `100ms` 执行 `.scrollIntoView()`，保证定位滚动的准确率。
3. **Stripe 层叠样式突围（行高亮）**：
   - 解决了在 `@keyframes` 中加入 `!important` 导致高亮呼吸背景色被浏览器忽略的问题。
   - 采用 CSS 穿透与变量清空：
     ```css
     :deep(.el-table tbody tr.highlight-flash-row) {
       --el-table-tr-bg-color: transparent !important;
     }
     ```
     强制屏蔽 Element Plus 表格默认斑马纹对目标单元格的底色平铺，让 `row-flash` 闪烁动效及高亮上下指示器边框得以在真实页面上展现。

### 4.3 兼容性友好链接重构
- 升级了通知中心列表 (`Notifications.vue`)。引入了 `getFriendlyLink` 函数，可自动检测和解析早期的 `/orders/123` 类型的旧数据详情链接，将其无缝转换为新版的带高亮参数列表链接 `/orders?highlight=123`，确保新旧交替期间链接不报失效。

---

## 5. 🔐 权限配置与标准测试数据（Seeding）升级
- **图纸权限细分**：在系统权限矩阵模型中正式增加了 `drawings`（工程图纸模块）独立视图权限。
- **标准化数据填充 (`seed.py`)**：去除了之前在开发阶段由于使用 `random` 随机分配权限造成的测试账号权限混乱。现已锁定确定的赋权模板：
  - **`laowang`**：车间工人角色，对核心系统看板、订单、客户档案以及图纸拥有只读权限，无法进行增删改和库存分配。
  - **`xiaoli`**：高级设计师角色，拥有工艺流、订单编制、图纸版本变更及用料管理等完整读写编辑权限。
- **Sidebar 调整**：优化了侧边栏导航条顺序，将“系统设置”后置到底部，菜单陈列更加符合常规使用习惯。
