# 热流道 MES 系统 - V2.2 更新总结

这次 V2.2 版本做了一次大改动，不仅加了新功能，还把之前用起来很不爽的“日志看不懂”、“点链接直接白屏”、“找不到高亮数据行”等问题全给解决了。

---

## 1. 💬 操作日志说“人话”了
之前在操作日志里，“详情”一栏显示的都是一堆乱七八糟的后台接口路径（比如 `POST /orders/12/steps/34/advance`），挤在一起根本看不懂是谁干了啥。
- **这次改动**：我们在后台加了个翻译中间件。现在它会自动查数据库，把这些冰冷的接口路径翻译成大家能看懂的大白话。
- **翻译效果**：
  - 以前显示：`POST /orders/12/steps/377/advance`
  - 现在显示：`确认完成了订单「ORD-2026-0025」的「激光表面打标」工序`
  - 以前显示：`POST /orders/60/materials`
  - 现在显示：`为订单「ORD-2026-0025」分配了零配件用料`
  - 以前显示：`DELETE /documents/2`
  - 现在显示：`删除了工程文档「v1719600000000-drawing.dwg」`

---

## 2. 📢 通知警报能自己定规则了（再也不会乱发）
以前系统发通知很死板，现在管理员可以在“系统设置 -> 通知规则配置”里自己点点鼠标配规则了：
- **触发条件随便挑**：可以设置在“订单创建”、“订单完成”、“图纸上传好（设计完成）”、“零件库存不够了（库存报警）”时发通知。
- **过滤条件很灵活**：比如可以设置只有在“订单优先级是特急”时才发，或者“零件可用库存小于5个”时才警报。
- **通知内容能套模板**：消息标题和内容可以用 `{order_no}`、`{product_name}` 这种格式，发出去的时候系统会自动替换成真实的订单号和产品名。
- **想发给谁就发给谁**：支持选择发给“所有人”、“管理员”，或者直接在列表里勾选某几个具体员工。
- **界面更好用**：切换通知事件时，表单会自动清理不相关的输入框，防止配错。

---

## 3. 🔩 订单做完自动扣库存，工序撤回自动退回库存
我们把生产订单和配件库存彻底打通了，现在系统能自己管好库存，不需要人工去对了。
- **订单用料分配**：在订单详情页里加了个“分配用料”卡片。在里面选零件时，能直接看到物理总库存、已被别人预留的库存和目前自己还能用的**“可用库存”**，零件不够了直接报错不给配。
- **做完自动扣库存**：只有当工艺流程里的所有工序都干完（或者选择跳过）时，订单才算完工。订单一完工，系统自动把预留零件从总库存里扣掉，并记录库存已扣减。
- **撤回自动退库存**：如果发现搞错了，把已经完工的工序撤回了，或者手动把订单改回“进行中”，系统会把扣掉的库存自动加回去，并重新锁回“预留”状态，数据一点都不会乱。

---

## 4. 📂 图纸管理重做：支持多版本，旧图纸自动淘汰
之前订单详情里的图纸堆在一块，很难用。这次彻底重构了图纸管理板（`DrawingPanel.vue`）：
- **图纸自动分类**：分成“预览图”和“工程文件”两大类，点一下就能按 2D图、3D图、工艺文件来筛选，还能显示每一类有几个文件。
- **看图更方便**：图片格式的图纸鼠标放上去直接能看大图预览，还能看备注；其他工程格式的图纸列在表格里支持一键下载。
- **版本不会乱**：上传同类型图纸时，版本号会自动从 v1、v2、v3 往上加。新图纸上传生效后，旧版本的图纸会自动变成“已废弃”状态，确保工人看图时拿到的都是最新版。
- **磁盘不留垃圾**：在系统里删掉图纸时，服务器硬盘里的文件也会被同步删干净。
- **权限控制**：如果不是设计师（没有 drawings 权限），图纸卡片里的上传、编辑和删除按钮会自动隐藏，只能看不能动。

---

## 5. 🛠️ 解决两大顽疾：点详情白屏、跳转不亮不滚动
- **白屏防御**：以前如果某个订单或客户被别人删了，你又在通知中心去点它，详情页拿不到数据直接就报错白屏。现在加了兜底保护，如果数据不存在，会弹窗提示“该数据已被删除”，然后自动把你送回列表页。
- **跳转三件套（高亮、翻页、滚动）终于好用了**：
  - **自动翻页**：修复了分页器的监听事件，如果高亮数据在第3页，页面会直接翻到第3页，不再傻傻地停在第1页。
  - **自动滚动**：等页面数据完全加载出来后，列表会自动丝滑滚动，把那一行挪到屏幕中间，不用你手动去滑着找。
  - **行高亮**：解决了表格自带样式把高亮动画挡住的问题，高亮行会闪烁淡蓝色呼吸灯 3 次，并在上下加上醒目的蓝色边框。

---

## 6. 👥 重新分配了老王和小李的权限，测试更省心
我们修改了数据库初始数据（`seed.py`），给两位测试员工配好了固定的岗位权限，登录测试不用再抓瞎了：
- **老王（车间工人）**：
  - 用户名：`laowang`，密码：`123`
  - 权限：所有板块（看板、订单、库存、图纸、外协）**只能看，不能改**，更不能乱上传和改库存。
- **小李（工艺设计师）**：
  - 用户名：`xiaoli`，密码：`123`
  - 权限：订单、工艺流、库存和图纸（`drawings` 权限）**能看也能改**，可以录用料和改图纸版本。
- **侧边栏调整**：把“系统设置”放到了侧边栏的最底下，更符合大家的使用习惯。
