# 汇易通热流道管理系统 - V2.3 版本更新总结

V2.3 版本是一个全方位对系统进行**体验重构、功能闭环与架构补强**的里程碑版本。更新范围涵盖了：**跨页面高亮跳转、分页与种子数据重构、工序模板深度清理与“上传照片”实装、局域网多端接入支持、移动端布局完全重构**，以及**日夜主题模式自动联动引擎**。

---

## 1. 🔔 跨页面通知联动、自动翻页、滚动定位与呼吸高亮 (Notification Links & Scroll Highlight)
为了彻底打通消息通知与核心业务界面的闭环，实现了从通知一键直达且智能定位的效果：
- **分页与滚动定位**：点击未读通知跳转到“订单列表”或“库存管理”时，系统会在后端进行计算，精确定位该数据所在的分页并自动跳转对应页码。然后利用双 `requestAnimationFrame` 确保 DOM 妥善渲染，调用 `scrollIntoView` 平滑滚动至目标行居中。
- **全局动画共享复用**：提取并复用了通知中心的高亮闪烁逻辑，将其以 `row-flash` 形式引入到 `Orders.vue` 和 `Inventory.vue` 中。
- **Vue Scoped Scoping 修复**：解决了在 Vue Scoped CSS 中，因为 `@keyframes` 被加上 scoped hash 导致外部组件无法触发 el-table 内部 td 动画的缺陷。通过将 `@keyframes` 声明移入 `:global`，使组件能通过 `:deep` 顺利唤醒高亮过渡动画。
- **分页组件补全**：
  - 为防止数据量增加导致页面撑破，为**通知中心**和**库存管理**页面补全了前端分页逻辑与接口参数，实现前后端分页协同。
- **种子数据规模扩充 (`seed_mock.py`)**：
  - 种子数据中新增了 30 条当月内随机时间分布的未读消息，消息均关联了真实生成的订单与库存 ID，便于测试翻页高亮效果。
  - 补充了 20 条模拟物料商品（共 25 条），方便直接测试库存列表的分页功能。

---

## 2. 🔧 工序模板深度重构与“上传照片”完工闭环 (Process Flow Refactoring & Photo Upload Verification)
对系统中累赘的未实装字段进行了全面扫除，并完成了工艺工序确认的核心闭环：
- **冗余字段与逻辑清扫**：
  - 从前后端数据库表结构中彻底删除了“可并行”和“依赖”这两个已废弃字段，清除了所有相关的联动判定，使工艺模板更加紧凑。
  - 移除了完成条件中的“勾选清单”这一无用属性。
- **负责人录入机制升级**：
  - 改变了原本“负责人输入框需要手动打字”的设计。现在负责人输入框已被重构为动态下拉框，数据实时拉取自系统内的真实用户账号（`/users` 接口）。
  - 在前后端补充了负责人的“非空校验”，防止录入空负责人产生管理漏洞。
- **“上传照片”完成条件闭环实装**：
  - **交互实装**：当某个工艺步骤的完成条件被设定为“上传照片”时，操作工点击“完成工序”将不会直接通过，而是弹出一个专用的照片上传对话框。
  - **关联落盘与图纸同步**：上传的照片会被 FastAPI 接收并规范落盘，同时作为一条新的工程图纸记录（分类归为“工序拍照”）自动写入 `documents` 表。这也意味着操作工上传的工序现场照片会**自动同步并呈现在订单详情页的“图纸管理”版块**中，形成了完美的生产追溯链路。

---

## 3. 💻 局域网接入共享与 Docker 容器网络配置 (LAN Sharing & Docker Networking)
解决了局域网内其他设备（手机、平板）无法正常访问 MES 系统的网络配置痛点：
- **Vite 监听配置更新 (`vite.config.js`)**：将 Vite 的 `server` 配置修改为允许局域网监听，并配置了 `allowedHosts: true`，允许外网域名与局域网 IP 接入。
- **后端 API 映射动态化 (`docker-compose.yml`)**：去除了硬编码的本机 `localhost` 回环地址。现在前端容器的 `VITE_API_BASE_URL` 会被修改为宿主机的局域网实际 IP 地址（例如 `http://192.168.5.33:8080`），从而使局域网内其他终端（如手机浏览器）渲染前端页面时，能顺利将请求发送给局域网内的后端服务。

---

## 4. 📱 移动端自适应重构与 UI 细节体验改善 (Mobile UX Refactoring)
全面重构了前端对于窄屏和触控设备的适配（基于 Vue 3 活性 `isMobile` 侦听分流）：
- **侧边栏折叠挤压动画优化**：修复了侧边栏收缩时“汇易通热流道管理系统”挤成一团后才换行的难看转译动画。现在在收拢时会瞬间隐藏文字并以简洁的 `M` 图标居中展示。
- **左下角动态问候卡片**：在侧边栏左下角重新设计了用户模块。会根据当前设备时间（上午、下午等）动态呈现温馨问候语，并列出当前登录用户的姓名与系统角色徽章。
- **一级页面操作按钮样式统一**：对所有一级管理页面的顶部按钮（添加、导出、搜索等）的尺寸、间距、填充色和 hover 态进行了规范统一，确保工业品牌视觉一致性。
- **订单详情时间轴转译**：移动端下自动切换为大字体、大热区（`min-h-[44px]`）的**垂直排列卡片时间轴**，单手即可轻松点按完成。
- **用料明细卡片化（双轨渲染）**：手机上原本拥挤不堪的 `el-table` 在 `isMobile` 下会自动切换为用 Tailwind 重新绘制的**扁平化卡片列表**，提供极佳的垂直阅读体验。
- **图纸面板弹性堆叠**：重构了 `DrawingPanel.vue` 在移动端下的按钮堆叠方式，利用 `flex-wrap` 控制窄屏换行，防止在小屏幕上发生组件被横向切断的情况。

---

## 5. 🌗 沉浸式主题系统与移动视口安全区防御 (Theme Synchronization & Viewport Fixes)
彻底重构了日/夜间模式在移动端的呈现方式，带来更具沉浸感的原生 App 级体验：
- **防闪烁阻塞初始化**：在 `index.html` 头部注入原生 DOM 拦截器。确保在 Vue 实例挂载渲染前的“第一帧”，底色就是完全正确的深色或浅色，彻底解决加载瞬间的“白屏闪烁”缺陷。
- **100dvh 动态高度防御**：将外层包裹的 `h-screen` (100vh) 彻底转换为 **`h-[100dvh]`**。彻底杜绝了因手机 Safari/Chrome 的顶部地址栏上下滚动伸缩，导致网页底部“橡皮筋回弹”拉扯时，露出黑色底层画布的严重版面错乱 BUG。
- **双端无缝系统状态栏联动 (`theme-color`)**：
  - 系统现在会在切换模式时，利用原生 JS 动态插入或更新 `<meta name="theme-color">` 标签的内容。
  - **完美体验**：在移动端，当您切换深/浅色模式时，**手机最顶部的原生系统状态栏（刘海区）的底色也会在一瞬间跟网页背景融为一体**，实现 100% 的 App 级沉浸视觉。
- **实时系统侦听器机制**：
  - 在 `App.vue` 新增了原生的 `window.matchMedia` 事件实时回调绑定。
  - 若用户**从未手动强制指定过主题**（即本地缓存为空），只要用户在手机的系统下拉菜单中切换系统的深/浅色模式，网页主题就会产生**零延迟的实时动态跟随变色**，且不会影响日后手动覆盖的优先级。
- **硬编码黑底 BUG 修复**：排查并清除了 `index.css` 和 `App.vue` 根节点中错误写死的纯黑色类名（`bg-industrial-900`），解决了“日间模式某些区域依然黑底白字”的恶性穿透问题。并对订单时间轴的圆点补充了 `dark:` 前缀。

---

## 6. 🧹 项目冗余文件与历史目录清理 (Legacy Directory & Redundant File Cleanup)
为了保持代码仓库的纯净度，避免多端及多系统（Windows / macOS）切换开发时历史文件的混淆，对项目结构进行了深度清理：
- **历史后端目录彻底删除 (`server/`)**：
  - 彻底删除了使用 Node.js / Express + SQLite 架构的旧后端服务目录。当前系统已完全切换到基于 FastAPI + PostgreSQL 的 `server-python` 服务，此历史目录已无任何用途。
- **废弃临时脚本清理 (`server-python/`)**：
  - **`fix_slashes.py`**：删除了临时用于批量修复 FastAPI 路由结尾斜杠的脚本。该脚本含有 Windows 本地硬编码路径，且路由优化已彻底固化至代码库中，无需保留。
  - **`migrate_vendors.py`**：删除了用于向 `vendors` 表手动添加 `contact_methods` 字段的一次性 SQL 迁移文件，该改动已在当前数据库中全部生效并完成同步。

---

## 7. 📝 全套开发与使用文档重构对齐 (System Documentation Realignment)
为了保证开发文档的有效指导性，消除历史遗留技术栈信息对后续开发的干扰，对 [docs/](file:///Users/xlz/Documents/G1-V2/docs) 目录下的全部文档进行了同步重构：
- **消除历史技术栈误导**：彻底清理了原文档中关于 Express 路由层、Knex 数据库迁移器以及 SQLite 文件的安装和故障排除说明。
- **与现行架构完全对齐**：全面重写为基于 FastAPI + PostgreSQL + Docker Compose + Vue 3 的系统安装启动指南、端口配置参数及依赖库参考。
- **更新核心业务原理解析**：在文档中补充了 V2.3 实装的线性工序状态机、工序拍照落盘验证以及自动物理扣减库存与撤回回退逆向还原库存的系统原理解析。
- **修正内置账号凭证**：纠正了文档中关于内置测试账号（admin、laowang、xiaoli）默认登录密码的遗留描述，统一更正为最新的数据库 seed 设定值 `123`。
