# 汇易通热流道管理系统 - 项目更新点总结与局域网共享指南

本文档总结了近期项目的前后端核心改造内容，并为您提供了如何在局域网（LAN）内让其他设备（手机、平板、其他电脑）访问该项目的实操指南。

---

## 一、 项目更新点总结

本次迭代对系统进行了订单状态、时间格式、安全策略、消息体系等方面的全面对齐与安全升级：

### 1. 订单状态流程对齐 (Order Status Alignment)
* **状态精简**：全栈（包括前端页面、后端接口、数据库表结构及种子脚本数据）统一收窄并对齐订单状态，仅保留以下三种状态：
  1. `in_progress` (进行中)
  2. `completed` (已完成)
  3. `paused` (暂停)
* **无用状态清理**：完全清除了包括 `draft`（草稿）、`progress`、`terminated`（终止）、`aborted`（中止）在内的过期状态及相关转换引擎判断。
* **状态机交互**：[OrderDetail.vue](file:///Users/xlz/Documents/G1-V2/client/src/views/OrderDetail.vue) 顶部状态控制钮支持一键在“暂停生产”与“恢复生产”之间双向流转。

### 2. 界面联动与时间格式统一 (UI Design & Formats)
* **返回客户详情**：如果用户是从“客户详情”页面点击某一订单，跳转进入 [OrderDetail.vue](file:///Users/xlz/Documents/G1-V2/client/src/views/OrderDetail.vue) 后，页面右上角将动态渲染一个 **“返回客户详情”** 按钮，点击即可直达原客户面板。
* **全局时间格式**：全系统所有列表与详情的时间，统一格式化展示为 `YYYY-MM-DD HH:mm`（如 `2026-06-21 17:39`）。
* **外协备注栏**：在外协（委外）列表 [Outsourcing.vue](file:///Users/xlz/Documents/G1-V2/client/src/views/Outsourcing.vue) 中，新增了直观的“备注”信息列。

### 3. 数据种子脚本升级 (Seed & Mock Idempotency)
* **脚本幂等处理**：[seed.py](file:///Users/xlz/Documents/G1-V2/server-python/seed.py) 升级为完全幂等，二次执行时不会跳过已存在的默认用户，而是强制覆盖重置其密码、身份标签和权限设置，并修复了 `is_active=1` 默认值注入以防止登录拦截。
* **全新模拟数据**：[seed_mock.py](file:///Users/xlz/Documents/G1-V2/server-python/seed_mock.py) 升级为一键重新生成 5 名客户、3 项中文工艺流程模板、25 笔关联订单（均含有自动指派的步骤和工序执行人）、5 类中文库房物品、3 家外协厂与 3 条未读消息的完整测试环境。

### 4. 翻页效果及功能修复 (Pagination Fix)
* **翻页同步**：修复了“订单管理”表格分页器无效的 Bug。后端接口 `get_orders` 全面兼容前端分页器传递的 `page` 和 `limit` 参数，并由后端完成 `offset = (page - 1) * limit` 的自动计算。

### 5. 用户安全校验与管理员智能授权 (API Access Control)
* **API 级访问锁**：为 [/users](file:///Users/xlz/Documents/G1-V2/server-python/routers/users.py) 下的所有用户/权限 API 统一挂载了 `verify_admin` 权限校验依赖项。任何非管理员（如 `laowang`）尝试在前端越权跳转或在后台直接请求用户 API 时，后端均会强制拒绝并返回 **`403 Forbidden`** 响应。
* **权限表精简**：从普通的页面权限列表中移除了“用户管理”复选开关。普通用户的权限分配列表中不再显示该项，管理员的页面权限判断收拢为纯角色（`is_admin == 1`）逻辑判断。
* **提拔自动赋权**：在 [users.py](file:///Users/xlz/Documents/G1-V2/server-python/routers/users.py) 核心接口中，一旦普通用户被新建或修改提升为管理员，后台将自动清空其旧权限，并全量插入开启状态的 8 项主功能页面权限记录，消除管理人员操作后的权限“空洞”。
* **防止自我锁定**：管理员在编辑自己权限时，“用户管理”菜单及对应 API 交互始终强制开启，防止因为失误操作关闭自己权限而产生死锁。

### 6. 右上角小铃铛与未读通知高亮引导 (Notifications & Highlight Glow)
* **小铃铛下拉通知**：页面顶部栏加装小铃铛 `el-popover` 卡片，配以未读数红点角标。点击即时下拉拉取未读卡片内容，卡片尾部提供一键直达“通知中心”的通道。
* **静默状态轮询**：顶部导航栏组件以 30 秒为周期自动轮询未读计数，多页面无缝同步状态。
* **转跳高亮闪烁**：点击未读通知卡片后，页面会自动跳转至“通知中心”，并在到达后通过 `scrollIntoView` **平滑滚动并居中**该条通知，伴随 3 次淡蓝色背景与呼吸阴影交替闪烁的过渡动画，让用户能立刻锁定对应消息。
* **Vnode 卡死修复**：将 `App.vue` 下拉框中的 v-else 循环与 v-if 容器节点归类为单一 div 块级组件，消除了 Vue 3 核心引擎在 unmount 展平的 Fragment 时由于指针空指向引发的页面锁死现象。

---

## 二、 局域网内其他设备访问配置指南

由于前端采用容器化部署，目前默认把访问后端的 API 根路径（`VITE_API_BASE_URL`）写成了本机回环地址（`http://localhost:8080`）。
当您尝试使用局域网内的其他设备（例如您的手机或局域网内的另一台 PC）访问您 Mac 上的前端页面（`http://<您的Mac电脑IP>:5173`）时，该设备的浏览器在运行页面代码时，**会直接尝试请求它自己本机的 `localhost:8080` 后端服务**，导致接口因无法连接而报错崩溃。

若要支持局域网内其他设备正常使用，请按照以下 4 个步骤进行配置：

### 步骤 1： 获取您 Mac 电脑的局域网 IP 地址
1. 点击 Mac 左上角苹果图标 $\rightarrow$ **系统设置 (System Settings)** $\rightarrow$ **网络 (Network)**。
2. 找到您当前连接的 Wi-Fi 或以太网，点击 **详细信息 (Details)**。
3. 记录下您的局域网 IP 地址（例如：`192.168.1.104` 或 `192.168.31.50`）。
   > [!TIP]
   > 您也可以打开终端运行命令快速查询：`ipconfig getifaddr en0`

### 步骤 2： 修改 `docker-compose.yml` 中的环境变量
1. 打开项目根目录下的 [docker-compose.yml](file:///Users/xlz/Documents/G1-V2/docker-compose.yml)。
2. 找到 `frontend` 服务的 `environment` 部分，将 `VITE_API_BASE_URL` 的 `localhost` 修改为您第一步获取到的 Mac 本机局域网 IP：
   ```yaml
     frontend:
       build:
         context: ./client
         dockerfile: Dockerfile
       volumes:
         - ./client:/app
         - /app/node_modules
       ports:
         - "5173:5173"
       environment:
         - VITE_API_BASE_URL=http://192.168.1.104:8080   # <--- 修改为您的 Mac LAN IP 地址
       depends_on:
         - backend
       restart: unless-stopped
   ```

### 步骤 3： 重新编译并拉起 Docker 容器
在您的 Mac 终端中运行以下命令，销毁旧容器并重新拉起编译：
```bash
# 停止当前运行的容器
docker compose down

# 重新编译前端包并以守护进程模式启动所有服务
docker compose up -d --build
```
> [!NOTE]
> 容器启动后，因为前端的 `client/Dockerfile` 中默认已经写入了开启全网卡侦听参数（`--host 0.0.0.0`），您不需要额外修改端口暴露设置。

### 步骤 4： 局域网访问与连接验证
1. **确认同一局域网**：将您的测试设备（如手机）连接到与 Mac 相同的 Wi-Fi 局域网内。
2. **浏览器访问**：打开设备的浏览器，输入 Mac IP 与前端映射端口：
   ```text
   http://192.168.1.104:5173
   ```
3. **功能检查**：输入用户名 `admin` 和密码 `123` 登录，检查仪表盘数据、点击列表查看等操作。此时，所有 API 请求都会准确地转发至您 Mac 的后端服务（`http://192.168.1.104:8080`），局域网多端协同已正常建立！
